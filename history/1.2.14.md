# 版本 1.2.14 更新日志

**发布日期**: 2026-02-05

## 新增功能 / 修复内容

### 摇杆设置调整

**问题描述**：
- 用户希望每次打开网站时都询问是否显示摇杆，而不是记住之前的选择。

**修复方案 / 实现方案**：
1. 修改 `js/input/joystickDialog.js`。
2. 移除读取和写入 `localStorage` 中 `desktopJoystickPreference` 的逻辑。
3. 确保每次初始化时都调用 `showJoystickDialog()`。

**修改文件**：
- `js/input/joystickDialog.js`

**相关 PR**：
- 无
